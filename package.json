{
  "name": "slidemath-ws",
  "version": "3.0.0",
  "description": "SlideMath WebSocket Server — real-time sync via HTTP API (PHP backend)",
  "main": "websocket.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node websocket.js",
    "dev": "node --watch websocket.js"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "dotenv": "^16.4.0"
  }
}
